import { View, Text } from 'react-native';

export default function TrainingScreen() {
    return (
        <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
            <Text>Page Training</Text>
        </View>
    );
}